import { reducer, initialState } from './activity-search-result-detail-page.reducer';

describe('ActivitySearchResultDetailPage Reducer', () => {
  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
