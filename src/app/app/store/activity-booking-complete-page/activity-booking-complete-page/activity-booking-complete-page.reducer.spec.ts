import { reducer, initialState } from './activity-booking-complete-page.reducer';

describe('ActivityBookingCompletePage Reducer', () => {
  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
