import { reducer, initialState } from './activity-booking-information-page.reducer';

describe('ActivityBookingInformationPage Reducer', () => {
  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
