import { reducer, initialState } from './activity-search-result-option-page.reducer';

describe('ActivitySearchResultOptionPage Reducer', () => {
  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
