import { KoOnlyDirective } from './ko-only.directive';

describe('KoOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new KoOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
