import { NumOnlyDirective } from './num-only.directive';

describe('NumOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
