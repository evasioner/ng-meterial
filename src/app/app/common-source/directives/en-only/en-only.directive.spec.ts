import { EnOnlyDirective } from './en-only.directive';

describe('EnOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EnOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
